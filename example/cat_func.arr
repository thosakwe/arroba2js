# cat() can be used to join data

# Strings
"Hello" -> cat(", world!") -> print

# Appending to array
arr = ["foo"] -> cat("bar")
print -> arr.all

# Adding numbers
1300
    -> cat(37)
    -> cat(" is the number of hackers.")
    -> print
